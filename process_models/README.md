# Process Models Directory

This directory is mounted into the SpiffWorkflow backend container.
Place your BPMN process model files here.

The directory is mounted as read-only to the container at /app/process_models

